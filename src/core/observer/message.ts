const ErrorMessage = {
  ALREADY_MADE_SUSCRIPTION:
    'Passed observer already at work i.e it has been previously used to make a subscription.',
  INTERNAL_OBSERVER_ERROR:
    'Internal `createObservable` error, _subscriber isn"t initialized',
};

export { ErrorMessage };
